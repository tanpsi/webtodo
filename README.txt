See https://tanpsi.github.io/webtodo for running version. The tasks persist
between refreshes.

Shortcomings:
	- I could not add an option for deleting tasks yet. They can be cleared
	  all together by clearing localstorage for that page.
	- The order of tasks can be changed by moving them but it doesn't
	  persist.
